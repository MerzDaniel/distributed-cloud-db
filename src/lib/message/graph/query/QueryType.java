package lib.message.graph.query;

public enum QueryType {
    /**
     * Request a document by ID
     */
    ID,
    /**
     * Request a document by type (we don't have document types yet)
     */
    TYPE,

}
